/**
 * PostCSS 설정
 *
 * Tailwind CSS v4(@tailwindcss/vite 경유)가 필요한 모든 PostCSS 플러그인을
 * 자동으로 설정하므로, 여기에 `tailwindcss`나 `autoprefixer`를 포함할 필요가 없습니다.
 *
 * 이 파일은 필요한 경우 추가 PostCSS 플러그인을 넣기 위해서만 존재합니다.
 * 예:
 *
 * import postcssNested from 'postcss-nested'
 * export default { plugins: [postcssNested()] }
 *
 * 그 외에는 이 파일을 비워 두어도 됩니다.
 */
export default {}
